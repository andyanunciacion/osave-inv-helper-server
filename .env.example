PORT=4000

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Comma-separated list of origins allowed to call this API (CORS).
# Defaults to http://localhost:3000 (the frontend's dev server) if unset.
ALLOWED_ORIGINS=http://localhost:3000
